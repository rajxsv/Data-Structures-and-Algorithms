#include <bits/stdc++.h>
using namespace std;

int prata(vector <int> v , int n)
{
    int start = 0 ;
    int end = sum;


}

int main()
{
    int t;
    cin >> t;

    while (t--)
    {
        int n;
        cin >> n;

        vector<int> v;

        for (int i = 0; i < n; i++)
        {
            cin >> v[i];
        }

        sort(v.begin(), v.end());

        while()
        cout << prata(v, n) << endl;
    }

    return 0;
}