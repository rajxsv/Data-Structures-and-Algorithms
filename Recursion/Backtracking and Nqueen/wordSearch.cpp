#include<iostream>
#include <vector>
#include <string>
using namespace std;

vector<int> dx
vector<int> dy = {}
bool f(vector<vector<char>> &board,string &word,int index){
	if(idx==word.size()){
		return true;
	}

	if(board[i][j]==word[index]){

	}
}